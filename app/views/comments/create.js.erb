$("#brand_<%= @brand.id %> .comments-count").html("コメント数: <%= @brand.comments.count %>");
$(".comments").html("<%= j(render 'comments/index', brand: @brand) %>");
$("#comment_comment").val("");

$('.review-rating').raty({
    readOnly: true,score: function() {
    return $(this).attr('data-score');
    },
    path: '/assets/'
    });